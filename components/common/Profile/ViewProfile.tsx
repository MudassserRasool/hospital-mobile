import { ThemedText, ThemedView } from '@/components/themed-view';
import React from 'react';

const ViewProfile = () => {
  return (
    <ThemedView>
      <ThemedText>ViewProfile</ThemedText>
    </ThemedView>
  );
};

export default ViewProfile;
